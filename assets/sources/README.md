# Sources

Place editable 3D sources here. Recommended structure:

```text
assets/sources/
  tumbas-reales-de-sipan/
    obra-1-1-L/
      model.blend
      textures/
      photos/
```

The pipeline can also start from an existing `.glb` by using `source.model` in a manifest.
